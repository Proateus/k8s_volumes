apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tondu-pvc
spec:
  storageClassName: azurefile
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
